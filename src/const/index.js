const DEFAULT_COUNT_USERS = 5;
const NOT_EXIST_USER = 'This user does not exist!';
const BD_TABLE_USERS = 'Users';
const BD_TABLE_BOARDS = 'Boards';
const BD_TABLE_TASKS = 'Tasks';

module.exports = {
    DEFAULT_COUNT_USERS,
    NOT_EXIST_USER,
    BD_TABLE_USERS,
    BD_TABLE_BOARDS,
    BD_TABLE_TASKS,
}
